/* yywrap is called by yylex when it encounters the end of input.
   It should return 1 if there is no more input, or 0 if more input is available.
   The default behavior is to return 1. */

int yywrap(void) {
    return 1;
}
